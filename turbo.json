{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "dev-all": {
      "cache": false,
      "persistent": true
    }
  },
  "globalDependencies": [
    ".env.local",
    "tsconfig.json"
  ]
}
